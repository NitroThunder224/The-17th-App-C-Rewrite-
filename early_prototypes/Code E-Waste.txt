// If no modifier.
                    if (order->modifier == 0) {
                        if (order->size == SMALL) {
                            printf("\nAdded %d more small %s!\n", order->num, printable);
                        }

                        else if (order->size == MEDIUM) {
                            printf("\nAdded %d more medium %s!\n", order->num, printable);
                        }

                        else if (order->size == LARGE) {
                            printf("\nAdded %d more large %s!\n", order->num, printable);
                        }

                        else if (order->size == PIECES) {
                            printf("\nAdded even more %d piece %s!\n", order->num, printable);
                        }

                        else {
                            printf("\nAdded %d %s!\n", order->num, printable);
                        }
                    }

                    // If modifier is "deluxe".
                    if (order->modifier == DELUXE) {
                        if (order->size == SMALL) {
                            printf("\nAdded %d more small deluxe %s!\n", order->num, printable);
                        }

                        else if (order->size == MEDIUM) {
                            printf("\nAdded %d more medium deluxe %s!\n", order->num, printable);
                        }

                        else if (order->size == LARGE) {
                            printf("\nAdded %d more large deluxe %s!\n", order->num, printable);
                        }

                        else if (order->size == PIECES) {
                            printf("\nAdded even more deluxe %d piece %s!\n", order->num, printable);
                        }

                        else {
                            printf("\nAdded %d deluxe %s!\n", order->num, printable);
                        }
                    }

                    // If modifier is "meal".
                    if (order->modifier == MEAL) {
                        if (order->size == SMALL) {
                            printf("\nAdded %d more small %s meals!\n", order->num, printable);
                        }

                        else if (order->size == MEDIUM) {
                            printf("\nAdded %d more medium %s meals!\n", order->num, printable);
                        }

                        else if (order->size == LARGE) {
                            printf("\nAdded %d more large %s meals!\n", order->num, printable);
                        }

                        else if (order->size == PIECES) {
                            printf("\nAdded even more %d piece %s meals!\n", order->num, printable);
                        }

                        else {
                            printf("\nAdded %d %s meals!\n", order->num, printable);
                        }
                    }

                    if (order->modifier == SPICY) {
                        if (order->size == SMALL) {
                            printf("\nAdded %d more small spicy %s meals!\n", order->num, printable);
                        }

                        else if (order->size == MEDIUM) {
                            printf("\nAdded %d more medium spicy %s meals!\n", order->num, printable);
                        }

                        else if (order->size == LARGE) {
                            printf("\nAdded %d more large spicy %s meals!\n", order->num, printable);
                        }

                        else if (order->size == PIECES) {
                            printf("\nAdded even more %d piece spicy %s meals!\n", order->num, printable);
                        }

                        else {
                            printf("\nAdded %d spicy %s meals!\n", order->num, printable);
                        }
                    }

                    if (order->modifier == DOUBLE) {
                        if (order->size == SMALL) {
                            printf("\nAdded %d more small double %s meals!\n", order->num, printable);
                        }

                        else if (order->size == MEDIUM) {
                            printf("\nAdded %d more medium double %s meals!\n", order->num, printable);
                        }

                        else if (order->size == LARGE) {
                            printf("\nAdded %d more large double %s meals!\n", order->num, printable);
                        }

                        else if (order->size == PIECES) {
                            printf("\nAdded even more %d piece double %s meals!\n", order->num, printable);
                        }

                        else {
                            printf("\nAdded %d double %s meals!\n", order->num, printable);
                        }
                    }

                    if (order->modifier == TRIPLE) {
                        if (order->size == SMALL) {
                            printf("\nAdded %d more small triple %s meals!\n", order->num, printable);
                        }

                        else if (order->size == MEDIUM) {
                            printf("\nAdded %d more medium triple %s meals!\n", order->num, printable);
                        }

                        else if (order->size == LARGE) {
                            printf("\nAdded %d more large triple %s meals!\n", order->num, printable);
                        }

                        else if (order->size == PIECES) {
                            printf("\nAdded even more %d piece triple %s meals!\n", order->num, printable);
                        }

                        else {
                            printf("\nAdded %d triple %s meals!\n", order->num, printable);
                        }
                    }

                    // If modifier is "deluxe" + "meal".
                    if (order->modifier == DELUXE_MEAL) {
                        if (order->size == SMALL) {
                            printf("\nAdded %d more small deluxe %s meals!\n", order->num, printable);
                        }

                        else if (order->size == MEDIUM) {
                            printf("\nAdded %d more medium triple %s meals!\n", order->num, printable);
                        }

                        else if (order->size == LARGE) {
                            printf("\nAdded %d more large triple %s meals!\n", order->num, printable);
                        }

                        else if (order->size == PIECES) {
                            printf("\nAdded even more %d piece triple %s meals!\n", order->num, printable);
                        }

                        else {
                            printf("\nAdded %d triple %s meals!\n", order->num, printable);
                        }
                    }